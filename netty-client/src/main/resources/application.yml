server:
  port: 8091

spring:
  application:
    name: netty-client

  kafka:
    bootstrapAddress: kafka:9092 #kafka:9092
    consumer:
      group-id: testGroup
    topic:
      name: testTopic

netty:
  host: localhost # netty-server
  port: 9000
